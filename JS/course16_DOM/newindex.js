// named import
// import { sum, PI } from './sum.js'/

import * as sum from './sum.js'

// const summarize = require('./sum.js')

// default import
// import summarize from './sum.js'
window.onload = () => {


    // console.log(sum.sum())
    // console.log(sum.PI)
    // console.log(sum.default)
    // const button = document.querySelector('button')

    // console.log(button)

    // function downloadURI(uri, name)
    // {
        // var link = document.createElement("a");
        // If you don't know the name or want to use
        // the webserver default set name = ''
        // link.setAttribute('download', name);
        // link.href = uri;
        // document.body.appendChild(link);
        // link.click();
        // link.remove();
    // }

    // downloadURI('https://www.africau.edu/images/default/sample.pdf', 'dogovor')

    // const, let, () => {}, Array.map, forEach, filter, sort, ``, import/export

    // const number = [1,2,3,4,5,6,7]

    // console.log(Math.max(...number)) // Math.max(1,2,3,4,5,6,7)

    //
    // const sum = (...numbers) => {
    //
    //     console.log(numbers)
    //     return numbers.reduce((prev, cur) => {
    //         return prev + cur
    //     }, 0)
    // }

    // console.log(PI)
    // console.log(summarize(1 , 4 , 4, 5, 6, 78, 8, 9, 9))
}
